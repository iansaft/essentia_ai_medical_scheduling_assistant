[pytest]
testpaths = tests
pythonpath = src
addopts =
    -ra
    --strict-markers
    --cov=essentia_api
    --cov-config=.coveragerc
markers =
    integration: tests that require Docker and a disposable PostgreSQL container
